class ParametricSurfaceBuffers {
  constructor({ position, normal, uv, index }) {
    this.position = position;
    this.normal = normal;
    this.uv = uv;
    this.index = index;
  }
}

export default ParametricSurfaceBuffers;